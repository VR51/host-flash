# Ready Made Hosts File

The hosts-[DATE].zip file is here for use by anyone not able to compile their own hosts blacklist. [DATE] is the date of compilation.

Build credits for the hosts file repositories used to build the provided prebuilt hosts files are near the top of the hosts file.

1. Download hosts-[DATE].zip
1. Unzip hosts-[DATE].zip to reveal the file called 'hosts'
1. Compare your system's hosts file (see location notes below) with the newly unzipped hosts file
1. Replace the head of the new hosts file with the head of your system's installed hosts file
1. Replace the old hosts file with the new hosts file.

# Typical place to find your system's installed hosts file

Unix, Unix-like and POSIX: `/etc/hosts`
iOS, Apple Mac: `/private/etc/hosts`
Android:  `/system/etc/hosts`
Windows NT, 2000, XP,[5] 2003, Vista, 2008, 7, 2012, 8, 10: `%SystemRoot%\System32\drivers\etc\hosts`
